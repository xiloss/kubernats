apiVersion: apps.kubernats.ai/v1alpha1
kind: KeyValueStore
metadata:
  labels:
    app.kubernetes.io/name: kubernats
    app.kubernetes.io/managed-by: kustomize
  name: keyvaluestore-sample
spec:
  # TODO(user): Add fields here
